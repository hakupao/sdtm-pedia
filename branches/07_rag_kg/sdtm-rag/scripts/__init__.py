"""sdtm-rag scripts package.

Contains chunker implementations (chunkers/), ingest pipeline (ingest.py),
and dataset parser (parse_dataset.py) — all offline / batch tools.

Phase 1A.3 writer entry point.
"""
