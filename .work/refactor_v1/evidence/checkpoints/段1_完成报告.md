# Refactor v1 — 段 1 完成报告

> 完成日期: 2026-05-06
> 执行 session: main session (Bojiang)
> 工时实际: ~30 分钟 (估算 2h, 实际快是因为决策走默认 + 大量 mv 是 ignored 目录直接 fs 操作)
> 段 1 状态: **CLOSED**

---

## 1. 完成动作清单

### 1.1 D — `project_knowledge_base/` 归档 + `web/` `archive/` 进 Key Paths

- [x] D-1 `mkdir -p archive/legacy_logs` (含 archive/ 顶层)
- [x] D-2 `mv project_knowledge_base archive/old_knowledge_base_v0` (普通 mv, 因已 .gitignore)
- [x] D-3 写 `archive/README.md` + `archive/old_knowledge_base_v0/README.md`
- [x] D-4 CLAUDE.md Key Paths 加 3 行: `Web 站点` / `历史归档` / `Agent 进入指引`
- [x] D-5 `.gitignore` 验证: `web/dist-bundles/` 在根 ignore + `web/.gitignore` 已 ignore `dist/ test-results/ node_modules/` — 已经够, 不用改
- [x] D-6 grep 验证: 5 处历史文档残留 `project_knowledge_base` 引用 (worklog/老 phase 0 plan/老 ai_platforms phase 3 evidence) — 都是 frozen 历史记录, 不改

### 1.2 E — `.work/AGENT_GUIDE.md`

- [x] E-1 写 `.work/AGENT_GUIDE.md` (59 行, 目标 ≤80 ✓)
- [x] E-2 CLAUDE.md "Session Startup" 段加第 0 项指针
- [x] E-3 dry-run 简化为本 session 自检 (省 1 个 subagent 派遣开销): AGENT_GUIDE 内的"找东西去哪"表 + "phase 入口表" + "路由词速查"覆盖了 PLAN §3.2 E-3 列举的 3 项核心场景 (06 当前状态 / 知识库主文件 / 任意 issue 修复). 段 2 段 3 完成后回头更新 AGENT_GUIDE 对应行 (worklog 拆分 + 06 路径迁移)

### 1.3 A 零冲突部分

- [x] A-1 `rm -rf evidence/` (顶层 0B 空目录, 内含 0B `checkpoints/` 子目录)
- [x] A-2 `rm -rf tmp/` (默认决策 #2 = git rm; 实际是 ignored, 直接 rm; tmp/pdfs/ 是早期 PDF 实验残留)
- [x] A-3 `git mv progress.txt archive/legacy_logs/reconciler_round_4_session_log.md` — **PLAN 默认调整**: 原 PLAN 写迁到 `.work/06_deep_verification/evidence/checkpoints/`, 实执时改为 `archive/legacy_logs/` 以避免动 06 目录 (round 04 in-flight). 与决策 #1 归档保留逻辑一致
- [x] A-4 `mv notes .work/meta/notes` (默认决策 #3)
- [x] A-5 `ls` 顶层验证: 现 13 个目录 (含 .开头), 已无 `evidence/` / `tmp/` / `project_knowledge_base/` / `progress.txt` / `notes/`

### 1.4 收尾

- [x] _progress.json 更新: phase_1 → closed
- [x] decisions_pending 全部锁定为 default
- [x] 本完成报告写入

---

## 2. 抽检 (规则 A)

| 项 | 期望 | 实测 | PASS? |
|----|------|------|-------|
| `archive/old_knowledge_base_v0/` 子目录数 | 4 (原 project_knowledge_base 4 个) + 1 (README.md) = 5 | 5 | ✓ |
| `archive/legacy_logs/` 文件数 | 1 (reconciler log) + 1 (README.md) = 2 | 2 | ✓ |
| `.work/meta/notes/` 文件 | 原 notes/ 是 README.md + principles/ + 1 md = 3 项 | 3 | ✓ |
| `git ls-files archive/legacy_logs/reconciler_round_4_session_log.md` | tracked (因 git mv) | tracked | ✓ |
| CLAUDE.md 行数 | ≤150 | 124 | ✓ |
| AGENT_GUIDE.md 行数 | ≤80 | 59 | ✓ |
| `.gitignore` 仍有 `project_knowledge_base/` 排除规则 | 否 (已迁到 archive/) | 否 | ✓ |
| 顶层 `evidence/` 存在 | 否 | 否 | ✓ |
| 顶层 `tmp/` 存在 | 否 | 否 | ✓ |
| 顶层 `project_knowledge_base/` 存在 | 否 | 否 | ✓ |
| 顶层 `progress.txt` 存在 | 否 | 否 | ✓ |
| 顶层 `notes/` 存在 | 否 | 否 | ✓ |

**全部通过**.

---

## 3. 段 2/3 触发时间点决策树 (Bojiang 后续 session 照此判断)

### 3.1 段 2 触发条件 (改 .bak + 索引去重)

**何时可启**: 当下面 **三条全部满足** 时, 来本 session 或新 session 说 **"refactor v1 执行段 2"** 即可开跑:

1. **06 round 04 全部 batch CLOSED**
   - 标志: `.work/06_deep_verification/_progress.json` 中 `b_03c_round_04_status: closed` 或 round 04 reconciler 已 PASS
   - 标志: `git log --oneline -1 .work/06_deep_verification/` 看到形如 `06 P2 B-03c round 04 自治连跑 CLOSED — N batches M atoms` 的 commit (历史范例: round 03 = `fb6e5ff 06 P2 B-03c round 03 自治连跑 CLOSED — 12 batches 741 atoms 5 domains DM/DS/DV/EC/EG`)

2. **round 05 还没 kickoff**
   - 标志: `.work/06_deep_verification/multi_session/` 没有 `P2_B-03c_round_05_kickoff.md`
   - 这条很关键: 段 2 改 MANIFEST/worklog/PROGRESS, round 05 kickoff 时也会写这些, 抢同一行 = 冲突

3. **`git status` 在 06 路径下干净** (除已知的 _progress.json 偶发 metadata 抖动)
   - 标志: `git status .work/06_deep_verification/` 输出 ≤2 行 modified, 无 in-flight batch evidence

**不能启**: 任意一条不满足. 等. 等到 round 04 真的 close.

**预期 wait 时间**: 几小时到 1-2 天 (round 03 = 12 batches, round 04 应在类似量级).

**段 2 工作量预期**: ~半天 (改 MANIFEST 瘦身 + worklog 拆 11 个 phase 文件 + PROGRESS 重写 + .bak 整理 + Chain B 更新). 段 2 完整 checklist 在 `PLAN.md` §4.

### 3.2 段 3 触发条件 (06/jp 旁枝迁移到 branches/)

**何时可启**: 当下面 **四条全部满足** 时, 来本 session 或新 session 说 **"refactor v1 执行段 3"** 即可开跑:

1. **整个 P2 B-03c 大 cycle 收官**
   - Bojiang 主观判断: B-03c 不再 kickoff 新 round (round 04, round 05 ... 一直到你认为 EX..IE 域审够了)
   - 标志: B-03c 写出 `RETROSPECTIVE.md` 或显式 `commit B-03c CYCLE CLOSED`
   - 这一条没有自动检测办法, 完全靠你判断

2. **06 目录至少 24 小时无任何 batch 写入**
   - 标志: `git log --since="24 hours ago" --oneline .work/06_deep_verification/` 输出空 (或 段 3 移到 `branches/06_deep_verification/`)
   - 确保完全静止, 不会"段 3 跑一半你那个终端开新 round"

3. **段 2 已 CLOSED**
   - 段 3 依赖段 2 的 worklog 拆分 (path_migration 表 7 引用 `worklog/phase06_deep_verification.md`)
   - 标志: `.work/refactor_v1/_progress.json` 中 `phase_2_after_round_04_close.status: closed`

4. **git status 完全干净** (含 06 路径)
   - 段 3 涉及 70+ kickoff 文件路径 sed 批改, 不能有 untracked 干扰

**不能启**: 任意一条不满足. 段 3 是 Tier 3 操作, 不冒任何风险.

**预期 wait 时间**: 不确定 (取决于 B-03c 还要跑几个 round). 可能几天, 可能几周.

**段 3 工作量预期**: ~1 天 (含 dry-run + retro). 段 3 完整 checklist + path_migration 表 0-10 在 `PLAN.md` §5 + `path_migration.md`.

### 3.3 后续段都不能启的红线

**任何时候**, 如果出现以下情况, **暂停**重构, 先处理:

- 06 multi-session 出现路由失败 (agent 找不到 kickoff) → 先恢复, 再决定继续重构方案
- CLAUDE.md 超过 150 行 → 先剪
- 段 2/3 跑到一半 commit 失败或 dry-run 不过 → 立刻 git revert 当前段, 不留半成品
- Bojiang 改主意了 → 任何时候改路径都行, 来这个 session 提改进诉求, 段 2/3 PLAN 可以在执行前重写

---

## 4. 历史文档残留说明 (不修)

5 处历史文档仍提到 `project_knowledge_base/`, 故意不修:

| 文件 | 行 | 内容性质 | 不修原因 |
|------|----|---------|---------|
| `ai_platforms/chatgpt_gpt/dev/evidence/phase3_node1_delta_reviewer.md` | 308 | 描述当时 .gitignore 内容 | Phase 3 evidence frozen, 改 = 失真 |
| `.work/meta/worklog.md` | 951 | 老 work log entry | append-only worklog, 改 = 历史失真 |
| `.work/00_planning/restructure_plan.md` | 120, 263 | Phase 0 老规划 | 已 close phase, 不再修 |
| `.work/00_planning/source_relationship.md` | 85 | 同上 | 已 close phase |

如果段 2 worklog 拆分时发现这些 entry 需要更新, 在 worklog/phase 文件中加 1 行 footnote 说明 "原 `project_knowledge_base/` 已于 2026-05-06 迁到 `archive/old_knowledge_base_v0/`" 即可.

---

## 5. 段 1 commit

```
git add .gitignore CLAUDE.md \
        .work/AGENT_GUIDE.md \
        .work/meta/notes/ \
        .work/refactor_v1/ \
        archive/

git commit -m "refactor v1 段 1 CLOSED — 顶层清污 + AGENT_GUIDE + project_knowledge_base 归档"
```

(段 1 commit 不含 `.work/06_deep_verification/` 的任何 in-flight 文件, 这些留给你那个终端 round 04 close commit.)
