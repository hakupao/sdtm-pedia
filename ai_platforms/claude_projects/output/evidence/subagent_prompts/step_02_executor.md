# Step 2 executor Prompt
> 调用时间: 2026-04-17 11:22
> Agent: oh-my-claudecode:executor
> Model: opus

## Prompt 正文

## 上下文
你正在执行 SDTM Phase 6.5 Claude Project 压缩部署的 Step 2。
完整计划: `/Users/bojiangzhang/MyProject/SDTM-compare/ai_platforms/claude_projects/PLAN.md` §7.4 Step 2 + §4.3 算法说明。
工作目录: `/Users/bojiangzhang/MyProject/SDTM-compare`

上一 Step 已完成：`ai_platforms/claude_projects/scripts/count_tokens.py` 可用，实测 INDEX.md = 5032 tokens。

## 你的输入
- 源文件（只读）: `knowledge_base/INDEX.md`
- Token 目标: ≤ 3000 tokens（压缩率 ~40%）
- 决策参考: PLAN §3.3 分区 "INDEX.md (精简)" / §4.3 通用要点

## 你的产出
1. 脚本: `ai_platforms/claude_projects/scripts/compress_index.py`
2. 输出文件: `ai_platforms/claude_projects/output/01_index.md`
3. 跑完后用 count_tokens.py 测产出 token

## 压缩算法要点（PLAN §4.3.x 推论 + §7.4 Step 2）
优先删：
- ASCII art / 视觉装饰性边框
- 冗长的自然语言介绍段落
- 重复说明（同一入口在不同段落重复）
- 示例性"这里可以找到 X"的解释句

保留：
- 9 类文件路径（路由骨架必须完整）
- 每个分区的简短一句话描述
- 关键索引锚点（chapters/ model/ domains/ terminology/ 等顶层目录）
- VARIABLE_INDEX / ROUTING 的入口提示

允许：
- 合并多级目录树为单层 `- 路径 — 说明`
- 去除 markdown 视觉对齐（表格转紧凑列表也 OK）
- 压缩 frontmatter 到最小

## 强制要求（P1-P7）
- 源文件只读 (P5)，不修改 knowledge_base/ 任何文件
- 脚本输出第一行加 `<!-- generated by scripts/compress_index.py at <YYYY-MM-DD HH:MM> -->`
- 脚本第二行（作为 markdown 注释）加 `<!-- source: knowledge_base/INDEX.md -->`
- 脚本幂等（重复跑结果一致，timestamp 可固定为执行时刻）
- 不写 README / 说明文档

## 验证（你必须自测后再返回）
```bash
cd /Users/bojiangzhang/MyProject/SDTM-compare
python3 ai_platforms/claude_projects/scripts/compress_index.py
python3 ai_platforms/claude_projects/scripts/count_tokens.py ai_platforms/claude_projects/output/01_index.md
```

**接受条件**:
- 实测 ≤ 3000 tokens → PASS
- 实测 3000-3300 tokens (超目标 ≤10%) → 接受但需在报告中说明
- 实测 > 3300 tokens → 必须进一步压缩，或在报告中解释无法压缩到目标的具体原因

## 完成标准
最后打印一行:
`[Step 2 DONE] output/01_index.md: <N> tokens (target: ≤3000)`

并报告：
1. 脚本行数
2. 压缩的关键手法（≤3 句）
3. 任何被删除但可能有风险的内容（如果有）

## 不要做
- 不要修改 knowledge_base/ 任何文件
- 不要创建其他 output/ 下文件
- 不要修改 PLAN.md / _progress.json / trace.jsonl（主控负责）
- 不要写额外 Markdown 说明文档
- 不要解释过程，只报告结果