# D2 Reviewer Prompt — extract_examples_data.py + 09_examples_data_high.md

> 落盘: 2026-04-19
> 目标 agent: oh-my-claudecode:code-reviewer, model=opus
> 隶属 PLAN_V2.md §1 (规则 D 审阅隔离) + §6 Task D2

## 背景

主控在 Task D2 dispatch 了另一 executor subagent 写 `scripts_v2/extract_examples_data.py` (opus, agent_id aaeb38e3115269993), 已跑出 `output_v2/09_examples_data_high.md` = 112,697 tokens / 28 域, 幂等 md5 `4cf1c1c6ab167cc31a47cdda891ec50e`.

**已知 spec deviation (用户已 ack)**: token 超硬 cap 50K 到 112K (2.25×). 用户选 Option A = 接受 (因总 RAG 容量 ~3-4M, 112K = 3.2%). 你**不需要**再评估 cap, 专注脚本质量 + 产物完整性。

## 你的任务 (独立上下文复核)

## 1. 脚本代码审 (scripts_v2/extract_examples_data.py)

- 幂等性: 是否用 `datetime.now()` 或 wall-clock? 时间戳源是否 source mtime / 文件 hash? (预期: 非 wall-clock)
- 源只读: 是否仅 `open(path, 'r')` / `Path.read_text`? 是否有任何 `open(..., 'w'|'a')` 指向 knowledge_base/?
- 路径处理: `REPO_ROOT` 是否 `Path(__file__).resolve().parents[3]`?
- Tier=others 分支: 即使本次未跑, 代码路径是否完整 (exclude-list 解析 + 反选)? 是否支持纯文本格式 exclude-list?
- Domain-list 解析: 是否支持 D1_domain_list.md (从 `## 最终清单` 代码块抓) + 纯文本两种?
- Determinism: domain 迭代是否 sorted? 表格行是否保序?
- 正则/解析健壮性: 数据表识别规则是否正确 (识别 `|...|` 且有 `|---|...|---|` 分隔)? "see also"/"shared with" 识别规则是否仅命中目标行?
- 错误处理: 无 examples.md 是否记 log 而非 crash? Hard cap 超限是否 exit 1 + stderr "EXCEED_HARD_CAP"?
- 依赖: 是否只 import stdlib + tiktoken?
- Docstring: 是否说明算法 + CLI + 输入输出?

## 2. 产物内容审 (09_examples_data_high.md)

**抽样域 (5 个, 与主控抽样不重叠)**: **DM, DS, EX, PC, IS**

对每个抽样域:
1. Read `knowledge_base/domains/<X>/examples.md` (源)
2. Read `output_v2/09_examples_data_high.md` 对应段 (产出)
3. 逐项核:
   - 每个 `## Example N` 源 → `#### Example N` 产出 (数量一致, 顺序一致)
   - **<X>.xpt**、**supp<X>.xpt** 等粗体 XPT 标记原样保留
   - Markdown 数据表: 列数一致, 行数一致 (完整保留), 分隔符 `|---|` 在
   - 首段 description ≤ 2 行 (符合 prompt 要求)
   - `**Rows N-M:**` 解释行已丢弃 (预期行为)
   - "shared with" / "see also" 链接如果源有, 产出应保留一行
4. 输出每个抽样域的 verdict: `PASS` / `DEFECT <描述>`

## 3. 结构审 (09 整体)

- 首行 `<!-- generated by ... at <ISO> -->` 是否在 (时间戳源是否 deterministic, 看两次跑 md5)
- 每域前 `<!-- source: knowledge_base/domains/<X>/examples.md -->` comment 在
- 域间 `\n---\n` 分隔
- 28 域全覆盖 (可 `grep -c "^### " 09_examples_data_high.md` 对比 28)

## 4. 产出

落盘 `/Users/bojiangzhang/MyProject/SDTM-compare/ai_platforms/claude_projects/output_v2/evidence_v2/D2_review.md`:

```markdown
# D2 Review — extract_examples_data.py + 09_examples_data_high.md

> 复核日期: 2026-04-19
> Reviewer: oh-my-claudecode:code-reviewer (model=opus, agent_id <你的 id>)

## 结论
**PASS | CONDITIONAL_PASS | FAIL** (必须三选一)

## 1. 脚本代码审 (<=10 bullets)
- 幂等性: ...
- 源只读: ...
- ...

## 2. 产物内容审 (5 域抽样)
### DM
- Example 数量源 N 产出 N ✓/✗
- 表格列/行 ...
- ...
- Verdict: PASS | DEFECT

### DS, EX, PC, IS
(同上)

## 3. 结构审
- 首行 generated comment: ✓
- 28 域覆盖: ✓/✗ (grep 结果)
- ...

## 4. 关键发现
(1-2 段, 重点观察)

## 5. 下一步建议
- PASS → D3 (build_v2_stage.py --stage v2.2)
- CONDITIONAL_PASS → 主控可继续但记 tech debt
- FAIL → 主控决策
```

## 5. 不要做

- 不要修改脚本或产物
- 不要重跑脚本 (幂等已确认)
- 不要评估 token cap (用户已 ack)
- 不要重叠主控抽样域: **避开 AE, PP, MB, GF, CP**

## 6. 完成信号

在最终回复里给主控:
- `D2 review done: <PASS/CONDITIONAL_PASS/FAIL>, 5 domains sampled (DM/DS/EX/PC/IS), review at evidence_v2/D2_review.md`