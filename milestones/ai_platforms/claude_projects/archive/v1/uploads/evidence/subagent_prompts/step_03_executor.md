# Step 3 executor Prompt
> 调用时间: 2026-04-17 11:33
> Agent: oh-my-claudecode:executor
> Model: opus

## Prompt 正文

## 上下文
SDTM Phase 6.5 Claude Project 压缩部署 Step 3。
完整计划: `/Users/bojiangzhang/MyProject/SDTM-compare/ai_platforms/claude_projects/PLAN.md` §7.4 Step 3 + §4.3 + §3.2 D5/D6。
工作目录: `/Users/bojiangzhang/MyProject/SDTM-compare`

⚠️ **Step 3 是计划中的关键检查点 Step**——ch04 是所有推理的基础，任何压缩都会伤害推理能力。

## 源文件 token 基线（已实测）

| 文件 | 大小 | 策略 |
|------|-----|------|
| ch01_introduction.md | 2411 | 精简 30-50% |
| ch02_fundamentals.md | 2783 | 精简 30-50% |
| ch03_submitting_data.md | 4907 | 精简 30-50% |
| **ch04_general_assumptions.md** | **31315** | **完整保留 (字符数 ≥95% 源文件)** |
| ch08_relationships.md | 11980 | 精简 30-50% |
| ch10_appendices.md | 7129 | 精简 30-50% |
| **合计** | **60525** | **目标 ≤45000** |

非 ch04 合计 29210，需压到 ≤ 45000-29749 ≈ 15250，即非 ch04 部分整体压缩率 ~48%。

## 你的产出
1. 脚本: `ai_platforms/claude_projects/scripts/compress_chapters.py`
2. 输出文件: `ai_platforms/claude_projects/output/02_chapters.md`
3. 实测 token，报告

## 压缩算法（PLAN §3.2 D5/D6 + §4.3）

### ch04 (31315 → 保留 ≥95%)
- 原样拼接到输出，保留所有 `###`/`####` 小节层级
- 不删除任何 Specification 表、规则列表、数字编号 assumption
- 允许：删除 frontmatter 的冗余 metadata 行、合并空行、删除 PDF 页码引用(如 `(see page 47)`)
- ch04 在 output 中必须作为一个连贯小节 `## Chapter 4: General Assumptions`（或保留源标题），源路径注释 `<!-- source: knowledge_base/chapters/ch04_general_assumptions.md -->`

### ch01/ch02/ch03/ch08/ch10 (非 ch04)
优先删：
- PDF 页码引用 `(page N)`、图表编号指引、长篇介绍性例子
- "For example, the following..." 型举例段落（只保留规则陈述）
- 冗余 frontmatter (chapter number, source PDF ref)
- 重复叙述（同一规则在正文 + summary + conclusion 的三次陈述）

保留：
- 所有 `###` 和 `####` 三级/四级标题（结构骨架）
- 所有 Specification / Variable 表格
- 所有编号列表（1., 2., 2a., 2b., ...）和规则陈述
- 所有引用到变量名 (`AESEQ`, `AEDECOD`...)、CT Code (`C66742`...)、domain 代码的句子
- Cross-reference 段落（关联其他章节的导航）

## 强制要求（P1-P7 + 幂等性修复）

### ⚠️ 时间戳必须可重现（Step 2 教训）
Step 2 reviewer 指出 `datetime.now()` 破坏严格幂等。**本 Step 必须采用以下之一**：
- 选项 A（推荐）: 使用 **最大 source 文件 mtime** 作为时间戳（`max(os.path.getmtime(f) for f in sources)` → 格式化）
- 选项 B: 完全省略时间戳，第一行只写 `<!-- generated by scripts/compress_chapters.py -->`
- **禁止**: 使用 `datetime.now()` 或任何随运行时变化的值

这样重复跑脚本，output 文件字节级一致（可 MD5 对比）。

### 其他约束
- 源文件只读 (P5)
- 输出第一行: `<!-- generated by scripts/compress_chapters.py (source mtime: <YYYY-MM-DD HH:MM>) -->`（若选项 A）
- 每章节前加 `<!-- source: knowledge_base/chapters/<file> -->`
- 脚本幂等（验证: 跑两次，`md5 output/02_chapters.md` 一致）
- 不写 README

## 验证（你必须自测后再返回）
```bash
cd /Users/bojiangzhang/MyProject/SDTM-compare
python3 ai_platforms/claude_projects/scripts/compress_chapters.py
TOTAL=$(python3 ai_platforms/claude_projects/scripts/count_tokens.py ai_platforms/claude_projects/output/02_chapters.md | grep -oE '[0-9]+')
echo "Total: $TOTAL"

# 幂等性测试
md5_1=$(md5 -q ai_platforms/claude_projects/output/02_chapters.md)
python3 ai_platforms/claude_projects/scripts/compress_chapters.py
md5_2=$(md5 -q ai_platforms/claude_projects/output/02_chapters.md)
[ "$md5_1" = "$md5_2" ] && echo "IDEMPOTENT: PASS" || echo "IDEMPOTENT: FAIL ($md5_1 vs $md5_2)"

# ch04 字符数保留率（PLAN §5.1 C6: ≥95%）
CH04_SRC=$(wc -c < knowledge_base/chapters/ch04_general_assumptions.md)
# ch04 在 output 中的字符数：从 "<!-- source: .../ch04" 到下一个 source 或文末
CH04_OUT=$(awk '/<!-- source: knowledge_base\/chapters\/ch04/,/<!-- source: knowledge_base\/chapters\/ch(08|10)/' ai_platforms/claude_projects/output/02_chapters.md | wc -c)
python3 -c "print(f'ch04 retention: {100*$CH04_OUT/$CH04_SRC:.1f}%')"
```

**接受条件**:
- Total ≤ 45000 tokens → PASS
- Total 45000-49500 (超目标 ≤10%) → 接受并说明
- Total > 49500 → 进一步压缩非 ch04
- 幂等 PASS
- ch04 字符数 retention ≥95%

## 完成标准
最后打印:
```
[Step 3 DONE] output/02_chapters.md: <N> tokens (target: ≤45000)
idempotent: PASS/FAIL
ch04 retention: <X>%
per-chapter tokens: ch01=..., ch02=..., ch03=..., ch04=..., ch08=..., ch10=...
```

报告：
1. 脚本行数
2. 非 ch04 章节使用的压缩手法
3. 任何对 ch04 做的修改（frontmatter、空行合并等）

## 不要做
- 不修改 knowledge_base/ 任何文件
- 不修改 PLAN.md / _progress.json / trace.jsonl / compress_index.py（主控负责）
- 不写 README
- 不使用 datetime.now()
- 不解释过程，直接报告结果