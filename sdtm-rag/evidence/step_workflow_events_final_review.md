# 最终整分支审查 — `feat/study-workflow-events` (e68043d..2b34abd)

> 审查方: final-review (独立 agent, 未参与任何 Task 实现/审查) · 日期 2026-08-26
> 范围: 23 commit / 26 文件 / +4442 −65 · 未改任何文件 · 未派 subagent · 未重跑全量测试
> 红线: 全文零真名, 一律用题号 (`ev_qNN`) + 计数 + 形态指代

## 0. 一句话结论

**有条件合并。** 交付了三样真东西 (三个从未被解析的 sheet 进 catalog 且有互证闸 / 一处已
上线的生产缺陷修复 / 一条确定性事件查询能力 + 33 题尺子), 收口证据的诚实度显著高于本仓
平均水平。但有 **1 个 Critical 级的"读者会被误导"问题**: `resolve_events` **在全仓零生产
调用方**, 而收口证据的标题写着"接线", §6-14 写着"设计定位是要 union-add 进检索的" ——
一个没参与的人读完会认为这条通道已经在线上工作。合并前需修 4 条, **全部是文档, 零代码改动**。

## 1. 独立复算 (本次自跑, 非引用)

以下数字由我用本地 catalog + 题集独立复跑, 与收口证据逐位吻合:

| 项 | 证据声称 | 我复算 |
|---|---|---|
| 三池规模 | 14 / 77 / 110 | **14 / 77 / 110** ✓ |
| assignments 去重 form 数 | 21 | **21** ✓ |
| items 池 | 959 | **959** ✓ |
| 33 题命中 | 21/33 = 63.64% | **21/33 = 63.64%** ✓ |
| 逐类 | 0/5 · 1/5 · 5/5 · 4/7 · 6/6 · 5/5 | **逐格相同** ✓ |
| 未命中题 id | §3.1 列 12 题 | **12 题 id 完全一致** ✓ |
| 全局返回 / tp / precision | 259 / 28 / 10.81% | **259 / 28 / 10.81%** ✓ |
| 未命中题纯噪声 / 返回≥10 的题 | 157 / 10 | **157 / 10** ✓ |
| 带隐藏清单的 item | 231/959 | **231/959** ✓ |
| "有 hidden 但减法未生效" | 0 | **0** ✓ (§6-13 的独立佐证成立) |
| Tier 1b 让位影响面 | 231/959 item, 7/21 form, 单次最多让掉 39 | **231 / 7/21 / 39** ✓ |
| 短 item OID 覆盖缺口 | 10/959 OID, 11/110 目标失去 Tier 2 精度 | **10/959 / 11/110** ✓ |
| cap 不变性 | 33 题有/无 cap 命中判定完全一致 | **True** ✓ (仅 `ev_q06` 60→50 被截断) |

补测两项证据里没有的:

- **当前 12 个未命中题里, 只有 2 题返回空 (沉默), 10 题返回噪声** —— 与 §6-15 的披露方向
  一致, 且这是"当前态"而非"前后差", 比两个口径的 6/10 更直接。建议补进 §6-15。
- **`item_oid` 全局唯一 = True** (959/959) —— §6-16 那条"点名的 item 属多个 form 会连坐
  两个 form"的隐含耦合当前确实 0 例触发, 该记录属实。

## 2. Spec 覆盖 (逐条)

### §2 In scope 六项

| # | 条目 | 判定 | 依据 |
|---|---|---|---|
| 1 | 事件层入 catalog 三池 | ✅ | `build_catalog.py` 产 events/activities/assignments; 复算 14/77/110; ledger 同步扩展三 sheet |
| 2 | OID ↔ 人类可读名双向可查 | ✅ (带限定) | `_exact_oid_index` (OID 入口) 与 `_name_index` (名称入口) 双向都有; `oid_name_mapping` 类 6/6。**限定**: 两个方向的**输出**都是目标标识符 (`event:`/`activity:`/`assignment:`), 不是可读名本身 —— 需要拿名字的调用方仍须回 catalog 做一次 join。这不是缺陷, 但"双向可查"这个措辞比实际交付宽 |
| 3 | item 真实采集范围推导 | ✅ | `collect_scope()` 独立成模块; 231/231 减法非平凡 (我复算); 经 Tier 2 交付查询。**注**: 按 S3 裁定不进卡片/向量库, 只经确定性通道 |
| 4 | 修 `適用範囲` 语义反转 | ✅ | diff 确认标签改为 `非表示アクティビティ` + 注释写明"列名是 Hidden in activity, 旧标签语义相反"; ledger 记录重灌后索引侧 231 卡带新标签 / 旧标签 0 |
| 5 | event 侧 gold 题集 | ✅ | 33 题六类, `lint_gold --events-catalog` exit=0, 白送分闸 0/0, 题集零进 git |
| 6 | 接 `study_lookup` 确定性直查 | ⚠ **半落实** | 方法存在、有 12 条单测、有 33 题实测; **但全仓 grep `resolve_events` 零生产调用方** —— `rag.py:386` 走的是 `resolve()` (卡片通道), `main.py` / `run_eval.py` / `routing_signals.py` 都不碰 `resolve_events`。按"在 StudyLookup 上加一条确定性直查方法"读 = ✅; 按"接进服务路径"读 = ❌ |

### §5 五道闸

| 闸 | 判定 | 依据 |
|---|---|---|
| A 交叉核对 (110/21/109+1) | ✅ | 两条测试固化; 我复算 110 与 21; 109+1 由 `sorted(kinds)==[1,109]` 断言看守。⚠️-2 已澄清四数出自 PDF 封面摘要减法残余, **非自证** |
| B 引用完整性四条 | ✅ | 四条差集长度=0; 修复轮1 补 activity OID 全局唯一测试 (77/77) |
| C 转置一致性 | ✅ | activity 键 61/61 + **form 感知 82/82** (抽检方发现原闸粒度弱于措辞, 已升级); §1 已写明 `61` 是回归锁不是判别力来源 |
| D 卡片侧回归 | ✅ (但闸文本已部分失效) | D.1 逐卡 diff 在 T1/T4 执行; D.2 golden v2 三遍 87.50% 逐题 Δ0; D.3 重灌后三遍稳定。原空闸 (`git status` 查 gitignored 目录) 已换成 sha256 961/961。⚠ **spec §5.D 原文"只允许 適用範囲 行与新增采集范围行变化"的后半句在 S3 后已不适用**, spec 未标注 |
| E 尺子先于接线 | ✅ | T5 在 T6 派发前冻结; 冻结后的修改 (拿掉白送分) 由**非受益方**提出、方向是收紧、发生在 T6 派发之前 —— 三条理由都成立, 记录诚实 |

### §6 四条自毁条款

| 条款 | 判定 | 记录诚实度 |
|---|---|---|
| S1 (闸 A 不吻合则停) | 未触发 | ✅ 闸 A 全绿, 且**未调整期望值迁就实现** (期望是先于实现从 PDF 摘要导出的) |
| S2 (转置 <61/61 则停) | 未触发 | ✅ 61/61 + 82/82 |
| S3 (卡片侧任何非预期变化则退回) | **触发, 用户裁定执行退回** | ✅ **诚实且四处一致**: `failures/t4_step7_retrieval_regression.md` 标题即"FAIL, S3 触发, 用户裁定退回"; plan:797 写死引用纪律「不得写成『未触发』或『验收通过』」; `build_field_cards.py:165-168` 生产注释写全机制与数字; 守门测试 `test_build_cards_omits_collect_scope_row` 调生产 `build_cards` 反向可失败, **非装饰**。归因实验 (A 臂 = 仅 T1 改动 → 87.50% 逐题 Δ0) 是判读规则先于数据写下的, 处置正确 |
| S4 (卡片/doc 侧 ≥8/10 则本单元价值不成立) | 判过, 未触发 | ⚠ **数字诚实, 但 git 内不可复现判定**。过程完整: 人工判断被打回 (归档 `task5_s4_v1_human_judgment_rejected.md`) → 零 LLM 实测 → 白送分闸修复后重测 → 补测 doc 侧 → 三口径 9.1% / 21.2% / 27.3%, 语义核验真阳性并集 3/33。**但**: 阈值 80% 与"未触发"这个判定, 以及最终三口径数字, 只存在于 gitignored 的题集 NOTES; checkpoint §5 有那组数字却是以"旧通道基线对照"的身份出现, 全文无一处写"S4 阈值 80%, 实测 X, 未触发"; 失败归档明写"记录在题集笔记文件, 不重复摘录于此" —— 指向一个 git 里没有的文件 |

## 3. 控制方 deferred / parked 逐条 triage

### A. 必须合并前修 (4 条, 全部是文档, 零代码)

| # | 项 | 为什么必须 |
|---|---|---|
| **A1** | **收口证据必须写明 `resolve_events` 零生产调用方** | 见 §4 Critical-1。这是"不实的『已设闸』记录比缺闸更害人"的同族: 不是设了假闸, 是让人以为通电了 |
| **A2** | **plan 与代码漂移必须收口** | plan 三处写 `_MAX_EVENTS_TOTAL = 8` + 一段单层实现的示例代码 + **48 个 checkbox 全部未勾**。plan 在 `docs/` 是可发现的正式文档, 下一个人极可能拿它当设计基准。最省事的修法: plan 顶部加一行「⚠ Task 6 的实现已被两轮修复取代, 设计基准以 `evidence/checkpoints/study_workflow_events.md` §2 为准 (cap=50, 四层优先级)」+ 勾掉 checkbox |
| **A3** | **S4 判定补进 git** | checkpoint 补两行: 阈值 80%、实测三口径 (9.1%/21.2%/27.3%)、语义核验真阳性 3/33、**判定未触发**。现在这条自毁条款的结论只活在 gitignored 文件里, 等于没有留痕 |
| **A4** | **Chain B 收尾** | 分支零改 `docs/PROGRESS.md` / `.work/meta/worklog/` / `CLAUDE.md` Key Paths / `.work/MANIFEST.md`。按本仓约定 (CLAUDE.md § Session Wrap-up) 这是合并动作的一部分, 不是可选项。至少: PROGRESS 加一行 study 轨状态, worklog phase07 append 一条, Key Paths 加一行指向本 checkpoint |

### B. 可以带着走 (记录已足够, 不阻塞)

| 项 | 理由 |
|---|---|
| 名称 → `assignment:` 粒度入口未实现 (`ev_q06`/`ev_q09`) | **裁定质量高**: 拒绝反射性补齐、用 precision 10.81% 作为拒绝依据、且明确记成"未完成项"而非"结构性限制"。这正是上一次犯错 (Ruling P2 补 recall 无人算 precision) 的反面教材式修正 |
| Tier 1b "整 form 让位"非无损 (231/959 item, 7/21 form, 单次最多 39) | 量化完整、我复算吻合、当前 33 题丢 gold = 0 且明写"未触发 ≠ 设计上不存在"。**但**它与"名称→assignment 入口"是同一个 precision 取舍下的两半, 应一起立项 (见 C) |
| **全局 precision 10.81%** | 披露充分 (§0 一句话结论就带、§6-15 详列)。**当前零害, 因为无消费方**。⚠ 建议在 §6-15 加一句消费前置条件: **接线前必须先跑一次 precision 门槛评估**, 否则 union-add 会把 157 条噪声灌进检索 |
| cap=50 层内按 dict 插入序截断 | 已披露, 已用反例 (`ev_q06`) 修掉全称断言; 我复算 cap 不变性 True |
| 短 form OID 兼通用医学词 → Tier 1b 误召回 | 已定性为设计固有代价 (21 个 form_oid 里 7 个只有 2 字符), 抑制需语义能力, 与零 LLM 前提冲突。判断正确 |
| 10/959 短 item OID → 11/110 目标失去 Tier 2 精度 | 已量化, 我复算吻合, 且明写"仍可达, 只是拿到未减法全集" |
| fixture repr 泄漏边界 (`--tb=short` / `-q` / `--showlocals` / 整体 dict 比较) | §6-10 定量边界写得清楚, 且诚实标注 `-l` 下防护**完全失效**是已知限制而非"以为挡住了" |
| 类型 6 的 5/5 是循环证据 | **处置是本分支最好的一处**: 标题中性化 + 三处写明不作为证据 + 加粗"两条都要满足缺一不可" + 搬出真正独立的 Task 4 数据自洽佐证 (我复算 231/0 成立)。无需再动 |
| 12 题判定词 fact 的双向风险 / 6 题+4 题 fact 相同 | 已在 yml file header 统一披露且首次写出假阴性方向。带着走 |
| M-1 (commit message 分母 961 vs 959) / M-3 (report 计数法脆弱) / M-5 (行号抄错) / O2 / O-rr2-1 / O-rr2-2 / O-rr3-2 | 全部无害, 历史不可改或已被系统性方案 (junitxml) 取代 |
| M-4 (`study_rag_pilot.md` 仍写旧标签) | append-only 历史证据, 不改写是对的 |

### C. 应当另立项 (4 条)

| # | 项 | 建议形态 |
|---|---|---|
| **C1** | **`test_ja_tokenize.py` 的真名** | 我独立扫了一遍 (只输出计数与长度, 不打值): **2 处高置信** —— 某行含 1 个 11 字符真实 label; 另一行同时含 1 个 5 字符真实 item OID + 1 个 4 字符 label 命中 (即 ledger 记的 OID+label 成对)。另有 **3 处 4 字符 label 命中, 置信度低** (4 字符临床词汇很可能是通用词撞车, 不宜直接当泄漏结算)。⇒ 单独开一个**红线清理任务**, 连同 impl-t4 扫出的既有命中面 (`build_kg_viewer.py` / `build_meta.py` / `study_fixtures.py` / `test_build_catalog.py` / `test_parse_config_report.py` / `fixtures/sp5_real_sample/lb.csv`) 一起过一遍。**不塞进本分支** —— 全部早于本轮数月, 且需逐条判真假阳性, 属独立工作量 |
| **C2** | **`oidscan_evidence.py` 未接任何自动化** | 它是本轮为堵红线临时造的工具, 有 19 条单测、fail-closed (空目录 rc=2)、路径掩码、9 条 (路径,needle) 精确 allowlist —— 工程质量够, 但**只能靠人记得手跑**。本仓已有的教训是"纸面规则等于没规则" (spec §8 第 3 条此前就是纸面的)。建议单开: 接 pre-commit 或做成一条 pytest。连带处理它自己的 4 条 deferred (未接自动化 / `load_needles` 两套过滤路径 / `min_len` 只作用 label 池 / 反方向扫描未实现) |
| **C3** | **"逐条相减"替代"整 form 让位" + 名称→assignment 入口** | 这两条是**同一个 precision 取舍**的两半, 应合成一个带数据判据的单元 (先定 precision 门槛, 再决定加哪些入口), 而不是各自反射性补齐 |
| **C4** | **生产接线 (union-add 进检索)** | 若确实要让这条通道产生用户可见价值, 需要独立单元: 定接入点 (`rag.py` 的 study 引擎?) + 定 precision 门槛 + 建卡片侧不回归闸 (S3 的教训: 往检索里加东西会挤占) + 跑一次 study golden v2。**这是本分支真正的"下一步", 但它不属于本分支** |

## 4. 跨任务问题

### Critical

**C-1 — `resolve_events` 零生产调用方, 而收口证据的措辞让人读成已接线。**

- 事实: 全仓 `grep resolve_events` (排除 .venv) → 只有 `server/study_lookup.py` 自身定义与注释、
  `collect_scope.py` 的文档字符串、以及测试文件。生产装配点 `server/main.py:121` 建的
  `StudyLookup` 只被 `rag.py:386` 的 `resolve()` (卡片通道) 与 `routing_signals.py` 的
  `strong_hit()` 消费。`run_eval.py` 同理。**没有任何请求路径会调到 `resolve_events`。**
- 误导面有三处:
  1. checkpoint 标题: 「Task 6: study_lookup 事件通道**接线** — 收口证据」
  2. §6-14: 「但 `resolve_events` 的设计定位**是要 union-add 进检索的**」—— 现在时读法是"已经在
     union-add", 而实际是"设计上打算但没做"
  3. §0 一句话结论通篇在讲命中率与 precision, 一个字没提这条通道当前不被任何人调用
- 为什么算 Critical 而不是 Important: 全局 precision 10.81% 这个数字, 在"无消费方"前提下是**零害的
  学术指标**, 在"已接线"前提下是**线上污染**。读者对这两种前提的判断, 会 180 度改变他对
  10.81% 该不该紧张的结论。收口证据把最要紧的那个前提漏了。
- 修法 (纯文档, 5 分钟): 标题改「事件通道**实现 + 实测**」; §0 加一句「本单元交付的是
  `StudyLookup` 上的一个方法与它的实测, **尚未接入任何服务路径** (`rag.py`/`main.py`/`run_eval.py`
  均不调用), 接线属后续单元」; §6-14 的"是要"改成"若将来 union-add 进检索"。
- ⚠ 需说明: **plan 里 Task 6 本来就没排接线步骤** (Files 只列 `server/study_lookup.py` +
  测试 + checkpoint), 所以**实现方没有偷工**, 这是收口措辞与计划范围之间的落差。

### Important

**I-1 — plan 与交付代码漂移, 且 plan 是 `docs/` 下的可发现文档。**
plan 三处写 `_MAX_EVENTS_TOTAL = 8`, 示例代码是单层遍历版, 48 个 checkbox 一个没勾。而交付的是
cap=50 + 四层优先级 + form 索引 + item 索引。plan 在 S3/P3/P4/I3 几次都被回写过, 唯独 Task 6 的
两轮修复没有回写 —— 属流程末端疲劳, 不是判断错误。见 A2。

**I-2 — spec 未标注 S3 结果。**
spec §2.3 / §5.D / §7 任务 1 仍描述"采集范围进卡片、961 张卡片重新渲染"。S3 退回后, 卡片不再
渲染该行, §5.D 后半句的判据已无对象。退回本身在 plan / failures / 生产注释 / 守门测试四处记录得
很好, 唯独**没回到 spec**。spec 是这条链的顶端, 建议在 §2.3 与 §5.D 各加一行
「⚠ 2026-08-25 S3 触发, 用户裁定退回卡片渲染; 本条改由 catalog 三池 + `resolve_events` 交付」。

**I-3 — S4 的判定不可从 git 复现** (见 §2 表)。见 A3。

**I-4 — Chain B 收尾未做** (PROGRESS / worklog / Key Paths / MANIFEST 零改动)。见 A4。

**I-5 — precision 10.81% 缺"消费前置条件"这条硬约束。**
现在的写法是"必须和命中率一起引用"。这管住了**引用**, 没管住**使用**。建议加一句:
**接线前须先定 precision 门槛并复测**, 否则 12 题里 10 题的 157 条噪声会直接进检索候选。
(这一条与 S3 是同一个教训: 往检索里塞东西的代价必须先测。)

### Minor

- **m-1 可移植性**: 本分支新增的 `test_catalog_workflow_pools.py` 与 `test_parse_workflow_sheets.py`
  在 fixture 里直接 `resolve_study("st01")` 读本地真实 xlsx。干净 clone 上会 ERROR (fixture error,
  不是 skip)。我核过: 其余读 xlsx 的测试全走 tmp registry 或 monkeypatch, **只有这两个新文件是硬依赖**。
  本仓无 CI 故不阻塞, 但若上 CI 必须先给这两个加 skip 闸。ledger M-3 的判断准确, 我确认范围就是 2 个文件。
- **m-2 §0 缺口径提示**: §5 的"source-recall vs fact-recall 不可直接换算"警告写得很好, 但 §0 一句话
  结论只报 21/33 —— 而只读 §0 的引用者最多。建议 §0 那句加 5 个字「(source-recall 口径)」。
- **m-3 §7"引用纪律自查"是空自查**: 「全文搜索 "SDTM 映射已支持" / "TA/TE/TV/SV": **0 处**」——
  搜一个自己从没打算写的词, 命中 0 是必然的, 这条自查的判别力为零。首轮审查方已点过, 未改。
  不是不实陈述 (结论属实), 但它占着"自查"的名分而不提供保证, 与本仓"装饰性取证"的红线同族。
  建议要么删, 要么换成有判别力的自查项 (例如搜"已接线/已上线/已启用")。

## 5. 措辞诚实度总扫

我对全分支 26 个文件搜了「验收通过 / 已证明 / 已支持 / 有效 / 消除 / 无害」, 逐处判证据强度。

**撑得住的 (抽查复算)**:

| 措辞 | 出处 | 判定 |
|---|---|---|
| 「已证明 33/33 题在有 cap/无 cap 两种口径下命中判定完全一致」 | checkpoint §9 | ✅ 我复算 True, 且是有界陈述 (只说判定不变, 不说没损失相关性) |
| 「231/231 减法都真实产生了非平凡的差集」 | §6-13 | ✅ 我复算: 231 个带 hidden 的 item 全部产生真子集, "减法未生效"= 0 |
| 「231/959 item 触发, 涉 7/21 form, 单次最多让掉 39」 | §6-16 | ✅ 逐个复算吻合 |
| 「259 / 28 / 10.81% / 157 / 10」 | §6-15 | ✅ 逐个复算吻合 |
| 「11/110 目标失去 Tier 2 精度」 | §6-8 | ✅ 复算吻合 (我第一次用错度量得 0, 换成"减法后可达性"口径复现 11) |
| 「有效」出现 8 处 | checkpoint | ✅ 全部在引用语境或否定语境 (「不作为『减法有效』的证据」), 无一处是自我宣称 |
| 「已经消除」 | §6-4 | ✅ 已被订正为"**未**消除, 只有 form OID 这一条入口被消除" |
| 「验收通过」/「已支持」 | 全分支 | ✅ **0 处**自我宣称 (仅出现在 plan:797 的**禁令**里) |
| 「归因已证明它逐题 Δ0, 无责」 | failures/t4:138 | ✅ 有 A 臂对照实验支撑, 是实验结论不是推断 |

**撑不住 / 需改的**:

| # | 措辞 | 问题 |
|---|---|---|
| 1 | checkpoint 标题「事件通道**接线**」 | 没有接线。见 Critical-1 |
| 2 | §6-14「`resolve_events` 的设计定位**是要** union-add 进检索的」 | 现在时读法成立"已在 union-add"; 实际零调用方 |
| 3 | §7「引用纪律自查 … 0 处」 | 空自查, 判别力为零 (见 m-3) |
| 4 | §2 In scope 2「OID ↔ 人类可读名**双向可查**」(spec 原文) | 输出恒为目标标识符, 不返回可读名。spec 措辞比交付宽半格 —— 不是虚假声称, 但会让人以为能拿到名字 |

**一处必须记功的反面**: 本分支犯过**两次**同类 (死代码 guard 写成"已设闸" / 归因错误复发),
两次都被**独立第三方**抓出来并**在文档里明写"上一版是错的"** —— 没有一次是悄悄改掉。
`§3.1` 那段"归因纪律 (修复轮1教训, 修复轮2里同类问题复发过一次)" 是我在本仓见过的最诚实的
自我记录形态: 它把自己犯了两次同一个错这件事写在了结论的正上方。这条应该保留成范例。

## 6. 合并建议

### **有条件合并** — 条件是 4 条文档修改 (A1-A4), 零代码改动。

**它交付了什么真实能力 (而不是"跑通了")**

1. **一处已经在线上生效的生产缺陷修复**: 卡片标签 `適用範囲` → `非表示アクティビティ`。
   这是语义**相反**的错误, 影响 231 张卡片, 且已经过重灌索引落到 localhost:8000 (ledger 记录
   chromadb API 查得旧标签 0 / 新标签 231, 与磁盘侧一致)。这一项**单独就值得合并**, 而且它是
   S3 退回后**唯一活下来**的用户可见改动 —— 归因实验证明它逐题 Δ0 无责, 保住它是对的。
2. **三个从未被任何管线读过的 sheet 变成可查数据** (14/77/110), 并且带三道互证闸:
   闸 A 的四个数来自 PDF 封面摘要 (独立来源, 非自证), 闸 C 的转置一致性是 xlsx 内两处独立
   表示的互证。**这不是"解析跑通了", 是"解析对不对有独立参照物"** —— 本仓大多数解析工作没有这个。
3. **一条确定性推导 (`collect_scope`) 并有独立于尺子的正确性佐证**: 231/231 减法非平凡、
   "有 hidden 但活动不在该 form 分配里 = 0"。后者是从数据自身一致性导出的, 不依赖 gold,
   是全分支最硬的一块证据。
4. **一把 33 题的尺子** + lint 闸 + 白送分闸, 且经历了"发现尺子本身瞎了 → 收紧 → 重测"的完整
   修正。尺子本身是可复用资产。
5. **一条能答出 21/33 的确定性查询能力**, 对照旧通道语义核验后的 3/33 (口径不同, 只作方向性
   对照)。真实增量集中在 `repeating_rule` 5/5 与 `oid_name_mapping` 6/6 与 `item_collection_scope`
   5/5 —— 即"给标识符 → 找记录"这一类, 这正是它的能力形状。

**它留下的未完成项会不会让下一个人被误导**

- **会, 但只在两处**: ①以为通道已接线 (Critical-1); ②拿 plan 当设计基准会看到 cap=8 与单层实现。
  两处都是 A1/A2 能修掉的文档问题。
- **其余不会**。三桶归因、precision 10.81%、Tier 1b 非无损、循环证据禁令、cap 插入序、
  短 OID 撞词、覆盖面缺口 —— 这七条限制的写法都达到了"读者不需要重做实验就知道边界在哪"的水准,
  而且几处主动写了"当前未触发 ≠ 设计上不存在"这种正确的区分。

**收口证据是否足以让一个没参与的人正确理解边界**

- 补上 A1/A3 之后: **是**。
- 现在: **不完全**。缺两块拼图 —— "这东西还没通电"和"S4 是怎么判的"。前者会让人高估已交付状态,
  后者会让人无法从 git 验证这个单元当初凭什么被允许继续做下去。

**为什么不是"不合并"**: 唯一的 Critical 是措辞而非功能; 代码侧两轮规则 D 三方核验的全部发现
均已处置且经控制方独立复验; 我自跑的 12 项数字逐位吻合; 生产行为的净改动只有一个标签修复
(已验证无回归) 加三个新 catalog 池 (纯增量, 无消费方回归风险)。**把一个已验证的生产缺陷修复
扣在分支上等文档, 才是更大的损失。**

**为什么不是"直接合并"**: 本仓写死的硬教训是「不实的『已设闸』记录比缺闸更害人」。
"标题说接线但零调用方"与那条教训是同一族 —— 不是假闸, 是假通电。而本分支自己就为
"死代码 guard 假安全感"付过一次代价, 在同一个分支的收口文档上重演一次同型问题, 不该被放过。

---

## 7. 自扫结果

```
$ .venv/bin/python scripts/oidscan_evidence.py ../.superpowers/sdd/2026-08-25-study-workflow-events/final-review.md
```

见文末追加。

```
catalog     : sdtm-rag/data/study/st01/catalog.json (OID 1071 个 + label 645 个 →
              去重合并后 1696 个 needle; 已剔除纯数字/过短取值 + 19 个公开 CDISC 词汇)
targets     : final-review.md → 1 个文件

CLEAN: 0 处未在 allowlist 的 OID/label 命中 (1 个文件)
rc=0
```

**本报告零真名, 已程序化自证。**
