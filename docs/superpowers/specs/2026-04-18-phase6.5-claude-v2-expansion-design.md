# Phase 6.5 Claude Project — v2 扩容方案 (Design Doc, 中庸版)

> 创建日期: 2026-04-18 (revised)
> 触发: Step 14 容量实测 12%, capacity_research 修订 200K 假设, 用户挑战"为何不直接 90%"
> 上游: `ai_platforms/claude_projects/PLAN.md` §8 postscript + `capacity_research.md` §6
> 范围: Phase 6.5 Claude 路线 v2 扩容部署 (v1 → v2.5 五批渐进, 最终目标 ~50% 容量)
> 状态: 设计完成 (中庸策略修订), 待用户批准

---

## 0. 修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1 (initial) | 2026-04-18 上午 | 战略型 3 批 / 13 文件 / ~15% 容量 |
| v2 (中庸) | 2026-04-18 下午 | 中庸 6 批 / 17 文件 / ~50% 容量, terminology 部分展开 + RAG 衰减曲线 |
| **v3 (current, 中庸-事实修正)** | 2026-04-18 晚 | **修正 ch06 不存在 (knowledge_base/chapters/ 实际只有 ch01/02/03/04/08/10), 批 1+2 合并为"chapters 全展开"; 总批数 6 → 5; 文件数 17 → 16-18; v2.6 → v2.5 终态** |

**关键策略转变**:
- 不再以"够用"为目标, 而是以"建立 RAG 质量 vs 规模曲线"为目标
- v2 终态从 15% 推到 50%, 留 30-50% buffer 给对话/Phase 7/标准更新
- 70% 作为可能的 Phase 6.5+ 第二轮终态 (取决于 v2.4/v2.5 衰减观察)

---

## 1. 背景与目标

### 1.1 触发链

1. v1 上传 (192K) 显示 12% capacity, 与预期 95% 偏差 8 倍
2. capacity_research 修订: paid 套餐 RAG 自动扩容 ~3-4M
3. v1 在 T1-T8 全 PASS, 但 T3/T6/T7 出现"间接重建/兜底", 损失原文级精确度
4. 用户挑战: "为何不直接占到 90%?" (见 §1.4)

### 1.2 目标 (中庸 / 50%)

**主目标**:
- v2.5 终态 token ~1.3-1.5M (RAG 容量 ~50%)
- 在 v1 T1-T8 上不衰减, T3/T6/T7 升级为原文精确
- T9-T20 新增题集全 PASS (覆盖五批新增内容, 批 1 占 4 题, 批 2-5 各 2 题)
- 建立 RAG 质量 vs 规模曲线 (6 个数据点: v1 + v2.1-v2.5)

**辅目标**:
- 留 50% capacity buffer 给: 对话上下文、Phase 7 知识图谱注入、CDISC 标准更新、未来扩到 70%
- 输出 RAG 衰减观察报告作为 Phase 7 设计输入
- 失败回退路径完整 (任何阶段衰减 ≥2 题立即停)

### 1.3 非目标

- 不动 knowledge_base/ 任何文件 (P5)
- 不替换 v1 Project, 不删 output_v1_baseline/
- v2 不追求 90% 满载 (留 buffer + 边际收益不足, 见 §1.4)
- terminology Term 值不全展开 (1944K 中, v2 取 top 500 codelist 即可, 全展开留 Phase 6.5+ 评估)

### 1.4 为什么不 90% (取舍记录)

| 论点 | 90% 派 | 50% 派 (本设计采纳) |
|------|-------|------|
| 已付费容量 | "12% 浪费 88%" | RAG 容量不是"必须用满"的资源, 留 buffer 是质量保障 |
| 精确度提升 | "全展开后无需边界模板" | 70%+ 时低密度 terminology Term 值占比过大, 价值密度急降 |
| RAG 衰减 | "未实测, 不假设衰减" | 未实测就更要保守起步, 建曲线再决定终点 |
| 维护成本 | "全文上传, 无需脚本" | 仍需脚本: terminology 1944K 全上传超 RAG 索引能力, 必须分组+部分压缩 |
| 不可逆性 | "可删可重传" | reindex 等待 + 用户操作疲劳, 渐进上传是稳妥做法 |
| 边际收益 | "100% 信息密度" | 95% 查询命中前 30-50% 内容, 后 40% 是长尾 |

**结论**: 中庸 50% 是"高 ROI 区段的上界 + 可控风险的下界", 70% 留作下轮目标。

---

## 2. 范围 (六批扩容)

### 2.1 总览表 (5 批 / 修正后)

| 批 | 内容 | 估 token | v2.X 状态 | 累计 token | 累计容量 % |
|---|------|---------|----------|-----------|----------|
| 1 | **chapters 全展开** (撤销 ch01/02/03/08/10 精简, 含 ch08 §8.3 完整, 替换 02_chapters; ch04 保持完整不变) | +50-100K | v2.1 | ~270K | ~14% |
| 2 | examples 高频域数据表 (用户 20 + 打分 top 5-8) | +50-100K | v2.2 | ~360K | ~18% |
| 3 | examples 剩余域数据表 (其余 ~35 域) | +80-150K | v2.3 | ~490K | ~25% |
| 4 | terminology 高频 codelist Term 值 (top 200) | +200-400K | v2.4 | ~840K | ~42% |
| 5 | terminology 中频 codelist Term 值 (top 200-500) | +300-500K | v2.5 | ~1.3-1.5M | ~50% |

**软截止**: 任一阶段 v2.X 出现 RAG 衰减 ≥2 题, 立即停止后续批, 视当前状态为 v2 终态。

**为何合并旧批 1+2**: knowledge_base/chapters/ 实际只有 6 文件 (ch01/02/03/04/08/10), 不存在 ch06; 旧批 1 仅剩 ch08 §8.3 单独展开 (+5-10K), 与旧批 2 (撤销 ch08 精简) 必然重叠且文件冲突。合并后批 1 一次性把所有非-ch04 章节恢复完整, 干净无歧义。

### 2.2 批 1: chapters 全展开 (+50-100K)

**操作**: 替换 v1 的 `02_chapters.md`, 撤销对 ch01/02/03/08/10 的所有精简。

**输入** (knowledge_base/chapters/ 实际 6 文件):
- `ch01_introduction.md` (102 行) — 全展开
- `ch02_fundamentals.md` (174 行) — 全展开
- `ch03_submitting_data.md` (130 行) — 全展开
- `ch04_general_assumptions.md` (1395 行) — 保持 v1 完整状态, 不变
- `ch08_relationships.md` (439 行, 含 §8.3 RELREC + §8.4 SUPP--) — 全展开
- `ch10_appendices.md` (310 行) — 全展开

**保留原则**:
- ch04 完整保留 (字符数 ≥ v1 02_chapters 中 ch04 段 100%)
- ch01/02/03/08/10 全部恢复完整 (包括 v1 删除的举例段落、Specification 表全列)
- 每章节起始保留 `<!-- source: knowledge_base/chapters/chXX_*.md -->` 标注

**理由**: T3 (PC↔PP RELREC) 升级需 ch08 §8.3 完整规则; T11/T12 (背景章节) 需 ch01-03/10 完整定义。

**产物**: `output_v2/02_chapters.md` (替换型, 软目标 ≤90K, 硬上限 110K)

### 2.3 批 2: examples 高频域数据表 (+50-100K)

**圈定算法** (B方案 + 用户高频):

**必选 (用户 20 域)**:
DM, SE, DS, BS, BE, MI, GF, PR, CM, EX, TU, TR, RS, SS, DD, LB, FA, CE, MH, SU

**B方案打分补充** (`scripts_v2/score_domains.py`):

```python
score = 0.4 * normalize(cross_ref_count)   # VARIABLE_INDEX 引用次数
      + 0.4 * normalize(examples_tokens)    # examples.md 文件 token 数
      + 0.2 * (20 if domain in T_test_hits else 0)  # T1-T8 涉及域 +20

T_test_hits = {AE, PC, PP, EX, MH, LB}  # 从 v1 test_results.md 抽
```

取 top 5-8 域, 与用户清单合并去重, **预计补 5-8 域** (AE/PC/PP/VS/EG/QS/IE 等).

**最终高频域名单**: 25-28 域 (用户 20 + 打分新增 5-8)

**产物**: `output_v2/09_examples_data_high.md` (软目标 ≤30K, 硬上限 50K)

**算法细节**:
- 解析每域 examples.md, 仅保留 markdown table 段
- Variable Name | Value 双列, 去除冗长 description 叙述
- 保留 Example 标题 + 关键 Description (≤2 行)
- 保留共享 examples 链接 (EX/EC, MB/MS 等)

### 2.4 批 3: examples 剩余域数据表 (+80-150K)

**范围**: 63 域中除批 3 已覆盖的 25-28 域之外的 ~35 域。

**算法**: 同批 3, 但对 examples 内容更长的域 (>20K tokens) 启用"采样压缩":
- 每域取前 3 个 Example 完整 + 后续 Example 仅保留标题
- 共享 examples 仍保留链接

**产物**:
- 主目标: `output_v2/10_examples_data_others.md` (≤50K)
- 若超 50K 硬上限: 按 SDTM Class 拆分为 `10a_examples_findings.md` + `10b_examples_events_interventions.md`

**v2.3 文件数**: 13 (若不拆) 或 14 (若拆 1 次), 仍在软目标 ≤15

### 2.5 批 4: terminology 高频 codelist Term 值 (+200-400K)

**圈定算法** (`scripts_v2/score_codelists.py`):

```python
score = 0.5 * normalize(domain_ref_count)  # 被多少个 domain 的 spec.md 引用
      + 0.3 * normalize(term_count)         # codelist 内 Term 数 (中等优先, 太大太小都减分)
      + 0.2 * (50 if codelist in T6_T7_T8 else 0)  # 测试题涉及的 codelist 加成
```

取 top 200 codelist, 覆盖 terminology 91 个文件中预计 ~50% 的 token 量 (因为高频 codelist 内 Term 值多)。

**算法细节**:
- 完整保留 codelist 标题 + 描述 + 全部 Term 值
- Term 表保留 5 列: Code | Term | Synonyms | Definition (截断 ≤200 字符) | Related Domain
- 不展开 NCI Concept Description 长文本 (链接到 NCI 即可)

**产物**:
- 主目标: `output_v2/11_terminology_high.md` (≤200K, 硬上限 250K)
- 若超 250K: 按 terminology 三大子目录拆 `11a_terminology_high_core.md` + `11b_terminology_high_questionnaires.md` + `11c_terminology_high_supp.md`

**v2.4 文件数**: 14-16 (取决于是否拆 3 次)

### 2.6 批 5: terminology 中频 codelist Term 值 (+300-500K)

**范围**: 批 5 之外的 codelist 中, 打分前 200-500 名的 ~300 个 codelist。

**算法**: 与批 5 一致, 但对超大 codelist (Term > 100) 启用 "采样":
- 完整保留 Code + Term + Definition (截断 ≤100 字符)
- 不保留 Synonyms 大段
- 不保留 NCI Description 链接 (减少元数据膨胀)

**产物**:
- 主目标: `output_v2/12_terminology_mid.md` (≤500K, 硬上限 600K)
- 必拆: `12a_terminology_mid_core.md` + `12b_terminology_mid_questionnaires.md` (各 ~250K)

**v2.5 文件数**: 16-18, 触及软目标上限 15 → 接受溢出, 但低于硬上限 20

---

## 3. 架构

### 3.1 目录结构最终态 (v2.5)

```
ai_platforms/claude_projects/
├── PLAN.md                         (v1, 不动)
├── PLAN_V2.md                      (新, 本设计的执行手册, 5 批 step-by-step)
├── RETROSPECTIVE.md                (v1, 不动)
├── RETROSPECTIVE_V2.md             (新, 收尾时写)
├── capacity_research.md            (不动)
├── rag_decay_curve.md              (新, RAG 质量 vs 规模曲线观察, 一等产物)
│
├── scripts/                        (v1 脚本, 不动)
├── scripts_v2/                     (新)
│   ├── score_domains.py            批 2 域圈定打分
│   ├── score_codelists.py          批 4/5 codelist 打分
│   ├── rebuild_chapters_full.py    批 1 (chapters 全展开)
│   ├── extract_examples_data.py    批 2 + 批 3 (参数化, --tier high/others)
│   ├── extract_terminology_terms.py 批 4 + 批 5 (参数化, --tier high/mid)
│   ├── build_v2_stage.py           分阶段构建 (--stage v2.1/v2.2/.../v2.5)
│   └── compare_versions.py         A/B token 对比工具
│
├── output_v1_baseline/             (新, v1 11 文件 archive, 只读)
│   ├── 00_routing.md ... 08_terminology_map.md
│   ├── system_prompt.md
│   ├── upload_manifest.md
│   └── README.md (说明: 只读, 对应第一个 Project)
│
└── output_v2/                      (新, v2 当前生效产物, 滚动更新)
    ├── 00_routing.md               (v1 复制)
    ├── 01_index.md                 (v1 复制)
    ├── 02_chapters.md              (v2.1 替换为完整版, ch04 不变)
    ├── 03_model.md                 (v1 复制)
    ├── 04_variable_index.md        (v1 复制)
    ├── 05_mega_spec.md             (v1 复制)
    ├── 06_assumptions.md           (v1 复制)
    ├── 07_examples_catalog.md      (v1 复制, 仍作目录)
    ├── 08_terminology_map.md       (v1 复制, 仍作映射索引)
    ├── 09_examples_data_high.md    (v2.2, 批 2 产出)
    ├── 10_examples_data_others.md  (v2.3, 批 3 产出, 可能拆 10a/10b)
    ├── 11_terminology_high.md      (v2.4, 批 4 产出, 可能拆 11a/11b/11c)
    ├── 12a_terminology_mid_core.md (v2.5, 批 5 产出, 必拆)
    ├── 12b_terminology_mid_questionnaires.md (v2.5, 批 5 产出, 必拆)
    ├── system_prompt_v2.md         (随每批迭代修订)
    ├── upload_manifest_v2.md       (累计 token 表 + 5 阶段历史)
    ├── test_results_v2.md          (T1-T20 完整 A/B 矩阵)
    └── evidence_v2/                (新, 三层记录体系)
        ├── README.md
        ├── _progress.json          (当前 stage + 已完成批列表)
        ├── trace.jsonl
        ├── stage_v2.X_*.md         (5 阶段各 1 份)
        ├── batch_NN_*.md           (5 批各 1 份)
        ├── checkpoints/
        │   ├── ckpt_v2.1.md ... ckpt_v2.5.md
        ├── subagent_prompts/
        │   ├── batch_NN_executor.md
        │   ├── batch_NN_reviewer.md
        ├── failures/
        │   └── (任何 attempt FAIL 必归档)
        └── rag_decay_observations/ (每阶段后由用户回报数据)
            ├── obs_v2.1.md ... obs_v2.5.md
```

### 3.2 v1 baseline 冷藏

```bash
mkdir -p output_v1_baseline/
cp output/*.md output_v1_baseline/
md5 output/*.md output_v1_baseline/*.md  # 必须一致
```

- output_v1_baseline/README.md: "只读, 对应第一个 Claude Project; v2 build 任何阶段失败可作为回退源"
- output/ 目录原状不动 (避免破坏 v1 trace 完整性)

### 3.3 v2 文件数策略 (修订)

| 准则 | 软目标 | 硬上限 | v2.5 触发 |
|------|-------|-------|---------|
| 单文件 token | ≤ 50K | ≤ 250K (terminology 段例外) | 批 4/5 必拆 |
| Project 文件数 | ≤ 15 | ≤ 20 | v2.5 预计 16-18 (拆批 4 + 拆批 5), 在硬上限内 |
| 文件命名号段 | 00-08 v1 内容 / 09-12 v2 扩容 / 90-99 元信息 | — | 留 13-19 给 Phase 7 |

**Project 文件数 18 是否合理**: capacity_research Q7 "Claude.ai Project Knowledge 单文件 30MB / 文件数无明确上限"。社区观察未见 18 文件因数量本身导致问题。RAG 索引按 chunk 而非文件级。

### 3.4 System Prompt 修订要点

`system_prompt_v2.md` 在 v1 system_prompt.md 基础上, 每批后增量修订:

| 阶段 | System Prompt 新增/修订 |
|------|----------------------|
| v2.1 | 移除 v1 "ch01/02/03/08/10 精简, 详见原文件" 兜底句; 新增"02_chapters 已是完整版, ch08 §8.3/§8.4 等含 RELREC + SUPP-- 完整规则" |
| v2.2 | 新增"09_examples_data_high 提供 25-28 高频域数据表; 该域查 example 数据时优先 09" |
| v2.3 | 新增"10 (或 10a+10b) 提供其余 ~35 域 examples 数据表; 全 63 域 examples 数据现可命中" |
| v2.4 | 新增"11 (或 11a+b+c) 提供 top 200 codelist 完整 Term 值; CT Code 查询优先 11, 命中失败 fall back 到 08" |
| v2.5 | 新增"12a/12b 提供 mid 频 codelist; CT Code 查询优先级 11 > 12 > 08" |

每个阶段的 System Prompt 全文都归档到 `evidence_v2/stage_v2.X_*.md`。

---

## 4. 数据流 (五批构建流水线)

### 4.1 总流水

```
                    knowledge_base/ (只读)
                            │
        ┌────────┬──────────┼─────────┬───────────┐
        │        │          │         │           │
       批1      批2        批3       批4         批5
       │        │          │         │           │
   chapters  examples   examples  terminology  terminology
   全展开    高频 25-28  剩余 35   top 200     mid 200-500
   │        │          │         │           │
   ▼        ▼          ▼         ▼           ▼
   02       09         10        11          12a/b
   _chapters _examples  _examples  _term       _term
   (替换)   _data_high _data_     _high       _mid
                       others
                                                │
   每批后 build_v2_stage.py:                    │
   - 复制 v1 不变文件到 output_v2/ (首次)        │
   - 跑当批脚本                                  │
   - 修订 system_prompt_v2.md                   │
   - 累计 token + 写 upload_manifest_v2.md      │
   - 写 stage_v2.X_*.md evidence                │
   - 更新 _progress.json                        │
                                                │
                      用户上传 v2.X 到第二个 Project ◄───┘
                                  │
                                  ▼
                      用户跑 A/B 测试 (T1-T20 中相关题集)
                                  │
                                  ▼
                      用户回报数据 → 主控写入 test_results_v2.md
                                  │
                                  ▼
               PASS? ─ yes ─→ 进入下一批
                      │
                      no ─→ 衰减归因 → 回退 / 修订 / 终止
```

### 4.2 渐进上传协议 (5 阶段)

| 阶段 | output_v2/ 变化 | 用户上传操作 | A/B 测试范围 |
|------|----------------|------------|-------------|
| **v2.1** | 替换 02_chapters (其余 v1 11 文件不变) | 新 Project 上传 11 文件 | T1-T8 + T9-T12 (chapters 全展开) |
| **v2.2** | + 09 (高频 examples) | 上传 09 (1 文件) | T1-T12 + T13-T14 (高频 examples) |
| **v2.3** | + 10 (或 10a+10b) (剩余 examples) | 上传 1-2 文件 | T1-T14 + T15-T16 (全域 examples) |
| **v2.4** | + 11 (或 11a+11b+11c) (高频 terminology) | 上传 1-3 文件 | T1-T16 + T17-T18 (高频 terminology) |
| **v2.5** | + 12a + 12b (mid terminology) | 上传 2 文件 | T1-T18 + T19-T20 (mid terminology) + 终测 T1-T20 |

**用户操作总量估算**:
- 上传次数: 5 (每阶段 1 次, 每次 1-3 文件; v2.1 是首次全量上传 11 文件)
- 删文件次数: 0 (v2.1 是首次上传新 Project, 直接传完整版 02_chapters; 后续阶段都是追加)
- A/B 问答数累计:
  - v2.1: 12 题 (T1-T12, 含 4 道 chapters 新题)
  - v2.2: 4 题 (T13-T14 新增 + T1-T12 抽 2 题回归)
  - v2.3: 4 题 (T15-T16 + 抽 2)
  - v2.4: 4 题 (T17-T18 + 抽 2)
  - v2.5: 4 题 (T19-T20 + 抽 2) + 终测 T1-T20 全跑 = 24 题
  - **总计 ~48 题**

**衰减触发回退**: 如某阶段 T1-T8 出现 ≥2 题 v2.X < v1, 立即:
1. 主控写 `failures/regression_v2.X.md`
2. 调 reviewer 归因 (文件冲突? Prompt 未声明优先级? RAG chunk 召回?)
3. 用户在 Project 删除本批新增文件, 回退到 v2.(X-1) 状态
4. 主控决策: 修订本批后重做 / 跳过本批 / 视当前为 v2 终态

---

## 5. 验收标准

### 5.1 Layer 1: 自动检查 (脚本层, 每批后)

| # | 检查项 | 标准 | 适用批 |
|---|-------|------|-------|
| C1 | v1 baseline 完整 | output_v1_baseline/ 与 output/ md5 一致 | 一次性 (v2.1 前) |
| C2 | 不变文件未被修改 | output_v2/ 中未变更文件与 v1 md5 一致 | 每批 |
| C3 | 02 撤销精简 | ch01/02/03/08/10 字符数 ≥ 各源 90% | 批 1 |
| C4 | 02 ch04 仍完整 | 字符数 ≥ ch04 源 95% | 批 1 |
| C5 | 02 ch08 §8.3/§8.4 完整 | grep `## 8.3` + `## 8.4` + 子节标题数 ≥ 源 | 批 1 |
| C6 | 09 高频域覆盖 | 25-28 域全部出现 `### XX` | 批 2 |
| C7 | 10 (或 10a/10b) 剩余域覆盖 | 余 ~35 域全部出现 `### XX` | 批 3 |
| C8 | examples 全 63 域 | 09 + 10 合计 = 63 域 | 批 3 |
| C9 | 11 高频 codelist 覆盖 | top 200 codelist 全有 | 批 4 |
| C10 | 12 mid codelist 覆盖 | 200-500 名 codelist 全有 | 批 5 |
| C11 | 单文件 ≤ 250K | build_v2_stage.py 报告 | 每批 |
| C12 | 累计 token ≤ 1.5M | upload_manifest_v2.md 汇总 | 每批 |
| C13 | 文件数 ≤ 20 (硬上限) | ls output_v2/*.md \| wc -l | 每批 |
| C14 | 源路径标注 | 每个合并输出 `<!-- source: -->` 全覆盖 | 每批 |
| C15 | system_prompt_v2 优先级 | grep 当前阶段必有的优先级声明 | 每批 |

### 5.2 Layer 2: 渐进 A/B 测试

#### 5.2.1 测试题集 (T1-T20)

**继承 v1 T1-T8** (回归):
- T1 变量精确查询 (AE.AEDECOD Core?)
- T2 规则推导 (AE 严重程度变化记录方式)
- T3 跨域关联 (PC↔PP RELREC 4 种方法) ⚠️ 重点 (批 1 应升级, 因 ch08 §8.3 全文)
- T4 反向查询 (哪些域有 EPOCH)
- T5 路由准确性 (变量是否提交到 SUPP--?, 批 1 后应有 ch08 §8.4 完整支持)
- T6 边界-examples (AE Example 2 数据) ⚠️ 重点 (批 2/3 应升级)
- T7 边界-terminology (CT Code C66742 值) ⚠️ 重点 (批 4 应升级)
- T8 边界-不编造 (CV 域所有变量值范围)

**新增题集 (T9-T20)** — 批 1 占 4 题 (因合并旧批 1+2), 其余每批 2 题:

| # | 批 | 测试题 | 期望 (v2.X 上 PASS) |
|---|----|-------|--------------------|
| T9 | 1 | "ch01 中 SDTM 整体架构 + Foundational concepts?" | 完整段落 (v1 FAIL) |
| T10 | 1 | "ch02 §2.6 创建新 domain 的所有步骤?" | 完整列表 (v1 FAIL) |
| T11 | 1 | "ch08 §8.3 RELREC 数据集关系示例完整流程?" | 完整规则 + 示例 (v1 仅大纲) |
| T12 | 1 | "ch10 附录所有 entity 列表?" | 完整 (v1 FAIL) |
| T13 | 2 | "DM 的 Example 1 完整数据表?" | 表格 (v1 兜底模板) |
| T14 | 2 | "EX 剂量调整 Example 数据怎么写?" | 表格 (v1 兜底) |
| T15 | 3 | "RP 域 Example 数据?" (非高频域) | 表格 |
| T16 | 3 | "FT 域 Example 数据?" (非高频域) | 表格 |
| T17 | 4 | "C66742 codelist 所有 Term 值 + Definition?" | 完整 Term 表 (v1 兜底) |
| T18 | 4 | "AERELN codelist 全部 Synonyms?" | Synonyms 字段 |
| T19 | 5 | "FREQ codelist (中频) 全部 Term + Code?" | 表格 |
| T20 | 5 | "PROBLEM_TYPE codelist (中频) 全部值?" | 表格 |

#### 5.2.2 A/B 矩阵格式

每阶段后用户回填 (主控提供模板):

```markdown
## v2.X A/B 测试 (date: YYYY-MM-DD)

| 题 | v1 答案摘要 | v2.X 答案摘要 | 精度 (持平/↑/↓) | 备注 |
|---|------------|--------------|-----------------|------|
| T1 | ... | ... | 持平 | |
| T3 | 间接重建 | 引用 ch08 §8.3 全文 | ↑ | 批 1 升级生效 |
| ...
```

#### 5.2.3 PASS 条件 (每阶段)

| 条件 | 阈值 | 处理 |
|------|------|------|
| T1-T8 v2.X 不出新 FAIL | 必须 | 强制 |
| T1-T8 v2.X 衰减题数 | =0 | 进下批 |
| T1-T8 v2.X 衰减题数 | =1 | 写 regression evidence + reviewer 归因, 主控+用户决策是否进 |
| T1-T8 v2.X 衰减题数 | ≥2 | **立即停, 视当前为 v2 终态, 进入收尾** |
| 当批新题 (T9-T20 中相关 2 题) | 至少 1 PASS | 进下批 |
| 当批新题 PASS = 0 | — | 归因, 重做本批或跳过 |

### 5.3 RAG 衰减曲线 (一等产物)

`output_v2/rag_decay_curve.md` 在每阶段后由主控更新:

```markdown
| 阶段 | tokens | 文件数 | Capacity % | T1-T8 PASS | T9-T20 PASS | 平均答题精度 |
|------|-------|-------|----------|-----------|-----------|------------|
| v1   | 192K  | 11    | 12%      | 8/8       | N/A       | baseline   |
| v2.1 | 270K  | 11    | <实测>   | <实测>    | T9-12     | ↑/持平/↓ |
| v2.2 | 360K  | 12    | <实测>   | <实测>    | T13-14    | ...        |
| v2.3 | 490K  | 13-14 | <实测>   | <实测>    | T15-16    | ...        |
| v2.4 | 840K  | 14-17 | <实测>   | <实测>    | T17-18    | ...        |
| v2.5 | 1.3M  | 16-19 | <实测>   | <实测>    | T19-20    | ...        |
```

**用途**:
- Phase 7 设计 RAG 拓扑时直接复用此曲线
- 决定是否在 Phase 6.5+ 推到 70%
- 提供给社区 (capacity_research Q3 实测数据缺失)

---

## 6. 风险与对策

| # | 风险 | 概率 | 影响 | 对策 |
|---|------|-----|-----|------|
| R1 | 批 3 examples 单文件超 50K | 高 | 拆 a/b/c | 脚本检测自动拆按 SDTM Class |
| R2 | 批 4/5 terminology 文件超 250K | 高 | 必拆 | 脚本按 core/questionnaires/supp 三子目录拆 |
| R3 | RAG 检索质量随集合增大衰减 | 中-高 (未知区) | T1-T8 出现新 FAIL | 渐进上传, 衰减 ≥2 题立即停 |
| R4 | System Prompt 优先级被 Claude 忽略 | 中 | 09/02 冲突时引用错文件 | T11/T12/T17 显式验证优先级 |
| R5 | 用户上传操作错误 | 低 | v2 实际状态偏离设计 | 每批后用户回报 "已上传 N 文件", 主控核对 manifest |
| R6 | 批 5 累计超 1.5M | 中 | 接近设计 50% 上限 | build_v2_stage.py 在 1.4M 时告警, 主控决策是否压缩 |
| R7 | 写/审同 agent (违反规则 D) | 低 | reviewer 失效 | executor + code-reviewer 必不同 subagent_type |
| R8 | 抽检漏内容 (违反规则 A) | 中 | 信息丢失被遗漏 | 每批 reviewer 抽 N + 主控独立抽 N (与 reviewer 不同样本), N=5 |
| R9 | 失败 attempt 删除 (违反规则 B) | 低 | 失败数据丢 | evidence_v2/failures/ 强制写, build_v2 失败时自动归档 |
| R10 | terminology 高频圈定算法误差 | 中 | top 200 漏关键 codelist | 主控人工 review 名单, 必加测试涉及 codelist |
| R11 | RAG indexing 时间过长 (>1 小时) | 中 | 用户等待疲劳 | 用户回报 "已上传, 等 indexing", 主控建议先做其他事, 收到 indexing 完成再回来测 |
| R12 | 多次上传/删除导致 Project 状态混乱 | 中 | v2.X 实际内容与设计不符 | 每阶段后用户截图 Project 文件列表, 主控对比 manifest |
| R13 | 累计 5 批 + 5 测试用户疲劳 | 中-高 | 用户中途放弃 | 提供阶段性"喘息"时间, 允许用户决定跳批; v2.2/v2.3 之后即便不进 v2.4/v2.5 也是有价值的中间态 |

---

## 7. 与 v1 设计的对照差异

| 维度 | v1 (PLAN.md) | v2 (本设计, 中庸) |
|------|-------------|------------------|
| 容量假设 | 200K 硬约束 | RAG ~3-4M (50% buffer) |
| 目标 token | ≤ 195K | ~1.3-1.5M (50% capacity) |
| 文件数 | 11 | 16-17 |
| 核心策略 | 极限压缩 | 渐进扩容 + 建衰减曲线 |
| 测试方式 | 一次性 T1-T8 | 6 阶段 A/B (v1 vs v2.X) + T9-T20 |
| Project 数 | 1 | 2 (v1 baseline 永久保留) |
| 阶段数 | 1 上传 | 6 阶段渐进上传 |
| Evidence | output/evidence/ | output_v2/evidence_v2/ |
| RAG 衰减观察 | 无 | rag_decay_curve.md 一等产物 |
| Retrospective | RETROSPECTIVE.md | RETROSPECTIVE_V2.md |

---

## 8. 中庸策略的"提前终止"协议

设计明确允许在任意阶段提前终止, 视当前为 v2 终态。提前终止的合理触发:

| 触发 | 处理 |
|------|------|
| RAG 衰减 ≥2 题 | 立即停, 进收尾 |
| 用户决定 (疲劳/其他优先级) | 主控接受, 写 RETROSPECTIVE_V2 时记录"主动终止于 v2.X" |
| 当批新题 PASS = 0 且重做仍 0 | 主控分析: 是 RAG 容量瓶颈还是内容选错; 决策跳批或终止 |
| 累计 token 超 1.5M | 不允许进入下批, 视为 v2 终态 |

**v2.3 之后任何阶段的中间态都是有价值的成果**: 用户可在 v2.3/v2.4/v2.5 任一停下并视为完结。

---

## 9. 执行手册预告 (writing-plans 阶段会展开)

PLAN_V2.md (用 superpowers:writing-plans 写) 会包含:

| 内容 | 对应 v1 PLAN.md 章节 |
|------|--------------------|
| 5 批 step-by-step (每批含输入/算法/产物/验收/checkpoint) | §4.4 |
| Agent 调度模板 (executor / code-reviewer / verifier) | §7.2-7.3 |
| 上下文管理策略 (主控只保留摘要, 脚本细节给 subagent) | §7.3 |
| 并行调度策略 (批 2/3/4/5 内部可并行, 但跨批必须串行) | §7.5 |
| 失败处理流程 | §7.6 |
| Checkpoint 与用户互动 (5 阶段各 1 个 hard checkpoint) | §7.7 |
| Evidence 三层体系 (沿用 v1 模板) | §7.10 |
| 完结归档协议 (RETROSPECTIVE_V2 强制写) | §7.9 + Rule C |

---

## 10. 下一步

1. ✅ 用户批准本 design doc (中庸版)
2. 主控用 `superpowers:writing-plans` skill 把本设计转为 `PLAN_V2.md`
3. PLAN_V2.md 包含 §9 列出的 5 批 step-by-step
4. 用户批准 PLAN_V2.md → 进入执行 (从批 1 开始)
